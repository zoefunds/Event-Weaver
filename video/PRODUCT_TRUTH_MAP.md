# PRODUCT_TRUTH_MAP — EventWeaver

Every claim shown in the film must appear here first. Statuses: working / working with
limitations / simulated / mocked / planned / unclear.

| Claim or feature | Status | Evidence | Safe wording | Visual treatment |
| --- | --- | --- | --- | --- |
| Markets are ordered chains of dependent real-world conditions (2–12 steps) | working | `contracts/event_weaver.py`, README "How a market works" | "a market is a chain of conditions, not a single bet" | Real UI: Create page / ChainViz component |
| Chain resolves YES only if every step occurs in order; one broken link = NO | working | Contract state machine (README lifecycle diagram) | as stated | Animated state-machine diagram: OPEN → RESOLVING → RESOLVED_YES/NO |
| Users stake native GEN on YES/NO via MetaMask | working | `stake_yes`/`stake_no` are `@gl.public.write.payable`, exercised on StudioNet | "stake GEN through your wallet" | Real UI stake panel + MetaMask confirm modal (recorded/reconstructed, no real funds shown moving) |
| GenLayer validators independently fetch evidence URLs inside GenVM (`gl.nondet.web.render`) | working | README "Why this needs GenLayer"; verified live on StudioNet | "each validator independently reads the evidence page for itself" | ArchitectureFlow: URL → multiple validator nodes rendering independently |
| Validators reach consensus via `gl.eq_principle.prompt_comparative` on outcome booleans + confidence band | working | README, `docs/CONTRACT.md`; live StudioNet run: one-round MAJORITY_AGREE, zero leader rotations | "validators must agree on the actual verdict, not just the format" | ConsensusVisual: multiple reasoning traces converging to one verdict |
| Per-step reasoning + evidence summaries stored on-chain, shown in resolution report | working | `docs/images/market-detail.png`; contract stores reasoning strings | "the reasoning is written on-chain and visible to anyone" | Real screenshot: market-detail.png resolution report, reconstructed in Remotion with real text |
| Claim computes stake + pro-rata share of losing pool minus fees (1% protocol + 0.5% creator) | working | `claim()` in contract; direct unit tests cover settlement math | "winners split the losing pool" | EvidenceCard / MetricReveal showing fee breakdown as real numbers from README, not fabricated amounts |
| Withdraw emits a real native token transfer (`emit_transfer(..., on='finalized')`) | working | README "value-transfer path"; exercised live on StudioNet | "withdrawing sends a real on-chain transfer back to your wallet" | UI withdraw button + on-chain transfer icon; do NOT show a fabricated tx hash/explorer screenshot (none captured) |
| Automatic deadline resolution triggered by always-on backend resolver | working | README "Backend (24/7)"; resolver.js runs 60s loop | "after the deadline, resolution happens automatically — no one has to remember to check" | ArchitectureFlow: clock icon → backend trigger arrow → contract (backend labeled "trigger," not "decision-maker") |
| Backend never holds user funds/keys; writes go wallet→contract directly | working | README architecture section | "the backend can't touch your funds — every write comes straight from your wallet" | ArchitectureFlow: wallet arrow bypasses backend box, goes straight to contract |
| 24 passing direct unit tests, genvm-lint 3/3 clean, CI on every push | working | README "Testing & quality gates"; `tests/direct/` | "24 tests, clean lint, CI on every push" | EvidenceCard: simple pass/check row, real numbers only |
| Live StudioNet integration verified (create → stake → adjudicate real URLs → claim → withdraw) | working | README quality gates table | "the full lifecycle has been run end-to-end on GenLayer's public testnet" | Text overlay on architecture diagram, not a fabricated receipt |
| Runs on GenLayer StudioNet (testnet) | working with limitations | README deployed addresses (StudioNet) | "live today on GenLayer's public testnet" — never say "mainnet" or "production money" | Small persistent label "StudioNet · testnet" near any wallet/value UI |
| Mainnet / real-money settlement | planned | Not in repo; inferred roadmap only | "designed to move to mainnet as GenLayer's mainnet matures" | Roadmap-style closing beat only, clearly framed as future |
| A specific transaction hash / block explorer confirmation | unclear (not captured) | No screenshot or recording of this exists in repo or supplied materials | Do not display any specific tx hash — refer to "verified live on StudioNet" generically per README | Do not fabricate an explorer UI; if shown, must be the real README claim as text, not a rendered fake explorer |
| User adoption, TVL, funding, or team size | unclear (no data) | None in repo | Do not mention | N/A — omit entirely |
| Evidence sources are fully adversarially hardened / oracle-proof | unclear | README documents the mechanism, not an adversarial audit | "verified by independent validator consensus" — do not claim it is unhackable | N/A — do not make this claim visually or verbally |
