# EventWeaver API — 24/7 availability configuration.
app = "eventweaver-api-prod"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  PORT = "8080"
  # StudioNet's RPC caps at 30 requests/minute. The indexer costs
  # 2 + 2×markets reads per cycle — at 15s that alone exceeded the cap with
  # zero user traffic (chronic self-inflicted rate-limiting). 45s leaves
  # headroom for the resolver and real user requests (portfolio, live
  # market reads) to actually succeed.
  POLL_INTERVAL_MS = "45000"

[http_service]
  internal_port = 8080
  force_https = true
  # Never scale to zero — the indexer and API must run 24/7.
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    interval = "15s"
    timeout = "5s"
    grace_period = "20s"
    method = "GET"
    path = "/health"

# Always restart on failure — belt and braces with the in-process
# never-die posture in server.js.
[[restart]]
  policy = "always"
  retries = 0

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
