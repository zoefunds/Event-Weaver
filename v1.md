# EventWeaver V1

## Purpose

V1 moves EventWeaver's staking and payout rail from native GEN to test USDC on Base Sepolia. GenLayer remains the source of truth for market rules, positions, evidence-driven adjudication, fees, and payout calculations. Base Sepolia holds the USDC and executes user deposits, settlement allocations, and claims.

This split gives users a familiar stablecoin reserve while retaining GenLayer's decentralized intelligent-contract resolution.

## Deployed V1 components

| Component | Network or host | Address or URL |
| --- | --- | --- |
| EventWeaver intelligent contract | GenLayer StudioNet | `0x96727fd9E35036903B89829E1349dB5A83e7c48f` |
| EventWeaver USDC escrow | Base Sepolia | `0x23Aca542DFE6FEF14d29A5184818a954eafA7B9C` |
| Test USDC | Base Sepolia | `0x036CbD53842c5426634e7929541eC2318f3dCF7e` |
| Frontend | Vercel | https://eventweaver-orpin.vercel.app |
| Backend API | Fly.io | https://eventweaver-api-prod.fly.dev |

The backend and database run in the Fly.io `priscilla-george` organization. The legacy Fly database was retired and must not be used as a source of current market data.

## Architecture

```text
User wallet
  |\
  | \ Base Sepolia: approve USDC, stake, claim
  |  \
  |   EventWeaverEscrow
  |       ^
  |       | settlement transaction from the backend relayer
  |       |
  |   Backend API, indexer, resolver, and relayer
  |       |
  |       | reads final outcomes and payout allocations
  |       v
  +----> GenLayer EventWeaver intelligent contract
          create market, record YES or NO position, resolve evidence, calculate payouts
```

The backend has a narrowly scoped Base Sepolia relayer key. It can call escrow `settle`, but it cannot transfer USDC out of the escrow except through the contract's predefined settlement mechanism. A user always claims their own payout directly from the escrow contract.

## User lifecycle

1. A creator defines a causal-chain market on GenLayer with conditions, public evidence URLs, a deadline, and a confidence floor.
2. A participant selects YES or NO and enters a USDC amount.
3. The wallet approves USDC and calls `EventWeaverEscrow.stake(marketId, amount)` on Base Sepolia.
4. The same wallet records `stake_yes(marketId, amount)` or `stake_no(marketId, amount)` on GenLayer. Amounts are six-decimal USDC units.
5. At or after the deadline, the resolver requests adjudication. GenLayer validators inspect the declared evidence and publish the terminal result.
6. The backend relayer reads `get_base_payouts(marketId)` from GenLayer and calls escrow `settle` once for that market.
7. A winner calls escrow `claim(marketId)` through the frontend. USDC transfers directly to that wallet.

## Contract responsibilities

### GenLayer intelligent contract

- Owns market metadata, causal-chain steps, evidence reasoning, deadline state, position ledger, terminal outcome, fee calculations, and the final Base payout list.
- Provides `get_base_payouts(marketId)` for the relayer after terminal resolution.
- Does not custody USDC and does not send native GEN as a payout in V1.
- Uses its internal authenticated time source. Public methods do not accept caller-controlled timestamps.

### Base Sepolia EventWeaverEscrow

- Accepts USDC deposits with `stake(marketId, amount)`.
- Tracks a deposited amount, allocated amount, and settlement state per market.
- Allows the authorized relayer to call `settle(marketId, recipients, amounts)` once.
- Allows any recipient with an allocation to call `claim(marketId)`.
- Exposes `claimable(marketId, account)` as the live source of truth for a user payout.

## Frontend behavior

- Market creation and position recording use the GenLayer wallet client.
- USDC approval, deposit, and claim use the injected EIP-1193 wallet on Base Sepolia.
- Base Sepolia transaction gas is explicitly bounded to avoid an RPC gas-estimation response that exceeds Base Sepolia's maximum transaction gas limit.
- Claim actions wait for the Base Sepolia transaction receipt before reporting success.
- Portfolio and market detail pages read Base escrow claimability through the backend. The claim button only appears when the live escrow amount is greater than zero.
- After a successful claim, the portfolio refreshes and the claim action disappears.

## Backend operations and reliability

The backend has four independent duties: serve indexed API reads, mirror GenLayer market data to Postgres, request adjudication for due markets, and relay final Base payouts.

StudioNet uses a shared, rate-limited RPC. V1 therefore uses five-minute polling for the indexer, deadline resolver, and Base settlement scan. The portfolio endpoint caches each address for 60 seconds. If a fresh StudioNet read is rate-limited, a previously cached portfolio is returned with `stale: true` instead of returning an error page.

The live Base escrow claim balance is still included in portfolio results, so an already confirmed USDC claim is not shown again merely because GenLayer retains a historical payout quote.

## Required configuration

Backend:

```dotenv
CONTRACT_ADDRESS=0x96727fd9E35036903B89829E1349dB5A83e7c48f
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
BASE_SEPOLIA_USDC=0x036CbD53842c5426634e7929541eC2318f3dCF7e
BASE_ESCROW_ADDRESS=0x23Aca542DFE6FEF14d29A5184818a954eafA7B9C
BASE_SEPOLIA_RELAYER_PRIVATE_KEY=<server secret only>
POLL_INTERVAL_MS=300000
RESOLVER_INTERVAL_MS=300000
```

Frontend:

```dotenv
VITE_API_URL=https://eventweaver-api-prod.fly.dev
VITE_CONTRACT_ADDRESS=0x96727fd9E35036903B89829E1349dB5A83e7c48f
VITE_BASE_SEPOLIA_USDC=0x036CbD53842c5426634e7929541eC2318f3dCF7e
VITE_BASE_ESCROW_ADDRESS=0x23Aca542DFE6FEF14d29A5184818a954eafA7B9C
```

Never put the relayer private key in source control, Vercel environment variables, browser code, screenshots, or documentation.

## Verification checklist

1. `GET /health` reports `ok: true` and the V1 GenLayer contract address.
2. The frontend bundle contains the V1 GenLayer contract, Base escrow, API URL, and USDC address.
3. A stake produces a USDC approval and escrow deposit on Base Sepolia, then a matching GenLayer YES or NO position.
4. A terminal market shows an escrow pool with `settled: true` after the relayer scan.
5. `claimable(marketId, wallet)` is greater than zero before a winning claim and zero after it.
6. The wallet's Base Sepolia USDC balance rises by the claimed amount.
7. Portfolio and market detail no longer display a claim button once `claimable` is zero.

## Migration and compatibility

V1 is not compatible with legacy native-GEN markets or the retired GenLayer deployment. Existing historical data should be treated as archival only. New V1 markets must use the deployed V1 GenLayer contract, V1 Base escrow, and the configuration listed above.

For deployment details, see [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md). For API semantics, see [docs/API.md](docs/API.md).
